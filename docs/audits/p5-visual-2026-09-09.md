# P5 family visual fixes and audit status

Date: 2026-09-09
Site: https://p5homeco.com
Source main commit: `8436b2d22af7958e4bdcdff111f74ee870018e47`

## Status

Implementation is prepared for review. The requested complete visual audit of every page, section, image and interaction on every device is **not complete**. These changes have not been deployed or visually verified in an edited-site preview. Keep the pull request in draft until that verification is complete.

## Changes

Solid quote-page call bar, safe-area spacing, readable supporting text and controls, flexible header and hero sizing, scrollable mobile menus and quote recovery dialogs.

New image paths are versioned to avoid immutable-cache collisions. Where a selected photo was reused in service, city, article or quote registries, its references were updated too. Existing image provenance and design-concept disclosures are preserved. The clothing edits are AI image edits of existing marketing assets, not evidence of additional completed client projects.

- `/images/p5-handyman-branded-v2.webp` (105688 bytes)

## Checks performed

- Edited TypeScript/TSX and CSS source parses successfully.
- TypeScript no-emit check passed.
- The shared slider component state regression passed for all four service-site implementations: mouse dragging, touch direction, taps, cancellation, secondary pointer rejection, keyboard increments, Home/End, endpoint clamping, and agreement between ARIA value and image clipping.
- The regression uses React test renderer with mocked image/icon rendering and geometry. It does not establish real Safari/Chrome touch behavior or visual layout quality.
- Selected live desktop homepage sections were visually inspected across all five sites, along with the Remodeling comparison interaction and selected marketing-image contact sheets. This is partial live-site coverage, not a full page-by-page audit.
- New WebP assets were decoded and visually reviewed at their generated dimensions. Cabinet's static image loader falls back to the original versioned file where no size variant is registered.
- A clean production build has not been established in this environment. Build prerequisites include operational scripts, additional repository assets and runtime capabilities outside the edited components.

## Remaining verification

The public sitemap contains 6 URLs for this site. A sitemap inventory is not proof that those pages have been visually reviewed. P5's central audit inventory also needs expansion for public routes omitted from the sitemap, including quote detail and confirmation routes.

The supported local preview server ran, but the browser rejected its address with `net::ERR_BLOCKED_BY_CLIENT`. No alternate browser or network path was used. Live desktop pages remained accessible. Responsive verification of these edits is therefore outstanding.

Before merge and deployment:

1. Open every public route and inspect every section in the edited build at widths 320, 375, 390, 430, 600, 768, 820, 1024, 1280, 1440 and 1920 pixels, including short landscape viewports and browser zoom.
2. Test every comparison with mouse, touch and keyboard in Chrome and Safari. Verify vertical page scrolling, pinch zoom, pointer cancellation, loaded images and a reachable handle at both extremes.
3. Verify bottom-bar background opacity, text contrast, tap targets, footer clearance, notched-device safe areas, software-keyboard behavior and suppression while a competing modal or estimator owns the bottom edge.
4. Complete the image inventory and inspect every remaining person representing a business for correct branding. Only the eight explicitly edited family assets have received clothing corrections in this pass.
5. Review every form and navigation flow without submitting test leads into production. Resolve layout findings, run repository build gates, then deploy and smoke-test the live sites.

## Reproduce the slider state regression

The P5 parent repository includes `scripts/qa/p5-slider-regression.cjs`. Check out the four service repositories beside the parent repository and install the parent's dependencies. In a temporary test-runtime folder with a `package.json`, install `react@18.3.1` and `react-test-renderer@18.3.1`. Run the script with `P5_AUDIT_ROOT` set to the folder containing the four repositories and `P5_TEST_RUNTIME` set to the temporary runtime folder. No production dependency changes are required.
